# CloudWatch Configuration
# Copy this file to .env and fill in your AWS credentials

# AWS CloudWatch Credentials (Required for error logging)
VUE_APP_AWS_ACCESS_KEY_ID=your_aws_access_key_here
VUE_APP_AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
VUE_APP_AWS_REGION=us-east-1

# CloudWatch Log Configuration (Optional - defaults provided)
VUE_APP_LOG_GROUP_NAME=acm-website-logs
VUE_APP_LOG_STREAM_NAME=error-stream
VUE_APP_ACTIVITY_STREAM_NAME=activity-stream

# Instructions:
# 1. Copy this file to .env in the project root
# 2. Replace the placeholder values with your actual AWS credentials
# 3. Ensure your AWS user has CloudWatch Logs permissions:
#    - logs:CreateLogGroup
#    - logs:CreateLogStream
#    - logs:PutLogEvents
#    - logs:DescribeLogGroups
#    - logs:DescribeLogStreams
# 4. Restart your development server after making changes

# For production deployment on EC2:
# Set these as environment variables instead of using .env file
# export VUE_APP_AWS_ACCESS_KEY_ID=your_key
# export VUE_APP_AWS_SECRET_ACCESS_KEY=your_secret
# export VUE_APP_AWS_REGION=us-east-1